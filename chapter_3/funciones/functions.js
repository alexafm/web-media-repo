// Family Tree exercise

'use strict';

function familyTree(myName, mumName, dadName) {
    console.log(myName + ' you are son of ' + mumName + ' and ' + dadName);
}

familyTree('Alexa', 'Alejandra', 'Ali');

// Distance between two points exercise


function distancePoints(x1, y1, x2, y2) {
    {
        return Math.sqrt {
            Math.pow(x2 - x1, 2) - Math.pow(y2 - y1, 2)
        };
    }
}

console.log(distancePoints);

// Money Exchange exercise


function moneyExchangeUsd(MXN) {
    var usdExchange = 19.50;
    var moneyExchangeUsd = MXN / usdExchange;
    return moneyExchangeUsd;
}

console.log(moneyExchangeUsd(400));
console.log(moneyExchangeUsd(508));
console.log(moneyExchangeUsd(630));
